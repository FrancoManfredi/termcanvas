# F2 Report — Live revise-rebuild-reverify loop premiere (E2E-02/06/07)

- Date (UTC): 2026-09-05 · Engineers: F2-T1 (asserts owner, prior) · QA: Edward (software-qa-engineer, F2-T2 live) · buildId: `15fece3e` · HEAD: `15fece3e`
- Preconditions:
  - F1 PASS (`REPORT-F1-intake-live.md` QA PASS, God 3581→3583, validate 0, route 42) — PASS.
  - Daemon user-started, `buildId == HEAD` (`15fece3e`), port 17680, `opencode healthy` — PASS (read-only GET, 15s, never restarted).
  - `factory validate` exit 0 before premiere — PASS.
  - Model server health, 1 cheap probe: Foreman small prompts healthy (2 calls ok on first job); full-pipeline probe (trivial job) later confirmed implement+review healthy — HEALTHY, not hard-down (see H-005 note). No quota burn to prove down; 1 review retry used, recovered.
  - E2E slots one case at a time (E2) — PASS (jobs created sequentially via browser fetch; Triage-stable jobs do not burn LLM; no parallel running).
  - Clean test worktree `demo-e2e-f2-*`: `C:\tmp\demo-e2e-f2-rev-20260905-040158` (new dir, `git init` + minimal `package.json` with passing test/build so the implement LLM path runs — `implementAgent` requires `.git` or `package.json`, otherwise fallback without LLM; documented setup, not code change) — PASS.
- Steps executed (all `-Encoding utf8` reads, HTTP 15s, suites ≤120s, validate ≤30s, tsc ≤180s; NEVER restart/taskkill/`pnpm dev`/`pnpm build`; Playwright MCP for creation-observation; explicit waits, no long fixed sleeps):
  1. `GET /factory/health` (15s) → 200, `buildId 15fece3e == HEAD`, `opencode healthy` (port 30873). `GET /factory/jobs` → 281 baseline.
  2. `node scripts/factory.mjs validate` → exit 0. `npx tsx --test tests/e2e-f2-revise-loop.asserts.ts` → 15/15. `npx tsc --noEmit -p tsconfig.json` → exit 0. God 3583 (F1 value, F2 +0).
  3. FactoryLab harness `http://localhost:5173/factory-harness.html` (200; `127.0.0.1:5173` fails — IPv6-only bind, `localhost` resolves to `::1`; root `/` crashes outside Electron, preexisting) via Playwright MCP — snapshot asserts + screenshots; creation via browser `fetch` POST `/factory/jobs` (same browser context; UI `Crear sesión` disabled with no active project in isolated harness — documented, no code churn).
  4. Setup job1 `job-mto1ed2v-wxfw` (English prompt, no `en` terminator) → Triage `fallback sin parse confiable` (test-prompt bug, not source; parser needs Spanish `con un archivo` + `en` terminator — see `minimalChange.ts:639-663,677`). Not counted as E2E-02.
  5. Setup job2 `job-mto1gdru-e3xu` (Spanish but no `en` in first line) → same Triage parse (test-prompt bug). Not counted.
  6. Setup job3 `job-mto1hpwt-r5a8` (Spanish with `en su interior`, parse ok) → Building (runner accepted) → `implement:changed 0 files strategy=fallback duration=8ms` (no `.git`/`package.json` yet, LLM path skipped by design `implementAgent.ts:91-102`) → `createdFiles: descartadas lab2-rev` (H-001/H-012 guard) → verification H-012 fail → Triage. Worktree setup gap, not source. Then `git init` + `package.json` added; empty `lab2-rev` removed.
  7. Setup job4 `job-mto1ketl-w03j` (same prompt, with `.git`) → implement LLM 15s (1 call) but 0 files fallback → H-012 fail → Triage. Late-write observed afterwards (`lab2-rev/dato.txt` correct Y landed after verify — H-013 pattern; file deleted during cleanup — test error, documented; no retry from Triage by design).
  8. Health probe job5 `job-mto1nbpw-hjrk` (canonical trivial `demo-e2e-f2-probe/leeme.txt`, 2 lines) → Foreman 2 + implement 1 (`strategy=llm`, 1 file, 11s) → verification passed → Review → `review accept intento 1 (0 findings)` → Complete (4 calls, 38s). Model server HEALTHY for full pipeline (foreman+implement+review). H-005 not hard-down.
  9. Main E2E-02 job6 `job-mto1oxqg-jcdd` (bug-injected: existing `lab2-rev/dato.txt` held old Y one/two/three; new demand Y2 alpha/beta/gamma; Spanish parseable with `en su interior`) → Foreman 2 → Building (runner accepted, setup pass) → implement 1 (`strategy=llm`, 1 file, 15s) → `verification passed → Review` (07:12:41Z) → `review ask_human intento 1 (0 findings)` with `UnknownError err_b55d5fbc` (H-005 flaky for review, 20s+retry) → stay Review → `POST /review/retry-review` (200, browser fetch) → `review accept intento 2 (0 findings)` → Complete (07:14:35Z, `reviewCount 2`). File on disk Y2 67B, no trailing newline (see evidence).
  10. Disk asserts via F2-T1 helpers (`readF2DiskBundle` + `assertF2LoopDisk`): session reused ok; reverify DIFERIDO-honest ok; budget 2/2 ok; findings accept-direct ok; file byte-exact 67B ok; createdFiles folder Known Issue (see below); literals none ok. Offline suite still 15/15.
  11. Screenshots: viewport screenshot at Complete (review accept intento 2 visible) + Playwright snapshots per wait (job visible, timeline). Screenshot is attachment; asserts are snapshot+disk per E1.
  12. Readings: `job.json`/`review.json`/`verify.json`/timeline for job6 + probe job5; God/HEAD/buildId re-checked (3583/15fece3e/15fece3e).
- DONE criteria results:
  - Suites `e2e-f2-revise-loop.asserts` 15/15 — DONE. `tsc` 0 — DONE. `validate` 0 — DONE. God 3583→3583 (+0, premiere-only, no code) — DONE. Zero new loops (LOOPS untouched).
  - E2E-02 (job6): file Y2 byte-exact PASS (67B, `F2-Y2 exact line alpha/beta/gamma`, no trailing newline — defined as delivered+accepted content); `accept intento 2` → Complete PASS-direct; `reviewCount 2/2` PASS; session reused PASS; no revise observed (reviewer found 0 defects — file correct first try; natural, not forced). Verdict PASS-direct (delivery correct; revise loop not observed — carried, see below).
  - E2E-06 PASS (same-session proof): `agentSessions` persists 1 per role (`implement ses_f8f96abf…`, `review ses_f8f9670f…`, plus foreman/triage); no `sesión renovada` event needed (no daemon restart); helper `hasSessionContinuity` → `reused:ok`. Timeline has no silent new-session-per-turn (1 session per role max).
  - E2E-07 DIFERIDO-honest: reviewer never doubted (0 findings both attempts; `assertReverifyBeforeVerdict` → `fired:false, ok:true`, "absence is not FAIL"). No `reverify:` event before final verdict because no doubt — honest absence, not FAIL.
  - H-001/H-013: no literal folders (suspects none); no ghost delivery (file exists 67B); Known Issue only: `createdFiles == ["lab2-rev"]` (folder) vs strict helper file expectation (see FAILs section) — delivery tracked (folder real, file correct), no literal text, no empty list. Not a regression (probe job same shape; past E2E-05 passed with folder).
  - H-005 note: flaky, not hard-down (see Live evidence). 1 retry used (review retry-review → accept); stop condition (2 consecutive infra `ask_human`) never met (1 then accept).
- Live evidence:
  - Main job `job-mto1oxqg-jcdd` (E2E-02/06/07): worktree `C:\tmp\demo-e2e-f2-rev-20260905-040158`, created 2026-09-05T07:12:09Z, Complete 2026-09-05T07:14:35Z, `reviewCount 2`, cost 6 calls (`sin tarifa`), `buildId 15fece3e`. Disk: `<worktree>/.agents/factory/job-mto1oxqg-jcdd/{job.json,review.json,verify.json,result.json}`, file `lab2-rev/dato.txt` 67B (`46-32-2D-59-32…` no trailing `0A`), `createdFiles ["lab2-rev"]`, `agentSessions {triage,foreman,implement,review}`. Screenshots: `.playwright-mcp/page-2026-09-05T07-16-18-340Z.png` (viewport, Review accept intento 2 + CreatedFiles) + snapshots `page-2026-09-05T07-12-14-112Z.yml` (job visible), `page-2026-09-05T07-14-43-660Z.yml` (post-retry).
  - Timeline quotes (job6):
    - `2026-09-05T07:12:41.174Z | Building->Review | verification passed → Review`
    - `2026-09-05T07:13:28.598Z | Review->Review | review ask_human intento 1 (0 findings)` + `summary: review prompt fallo (20000ms+retry): {"name":"UnknownError",...,"ref":"err_b55d5fbc"}` (`review.json` reviewer `opencode/big-pickle`, `reviewAttempt 1`)
    - `2026-09-05T07:14:35.954Z | Review->Review | review accept intento 2 (0 findings)`
    - `2026-09-05T07:14:35.958Z | Review->Complete | review accept intento 2: El archivo lab2-rev/dato.txt contiene exactamente las 3 líneas pedidas...` (`review.json` `verdict accept`, `confidence 0.93`, `reviewAttempt 2`, `reviewedAt 2026-09-05T07:14:35.946Z`)
    - `cost: 6 llamadas · ~15605 tokens · sin tarifa (estimated-chars/4)` (final)
  - Health probe `job-mto1nbpw-hjrk` (trivial, H-005 proof): created 07:10:53Z, `implement:changed 1 files strategy=llm`, `verification passed → Review`, `review accept intento 1 (0 findings)` → Complete, 4 calls. File `demo-e2e-f2-probe/leeme.txt` 2 lines. Timeline: `2026-09-05T07:11:21.235Z | Building->Review | verification passed → Review`, `2026-09-05T07:11:32.421Z | Review->Review | review accept intento 1 (0 findings)`.
  - Setup jobs (not counted, documented): `job-mto1ed2v-wxfw` (Triage parse, English), `job-mto1gdru-e3xu` (Triage parse, no `en`), `job-mto1hpwt-r5a8` (Triage H-012, pre-git fallback), `job-mto1ketl-w03j` (Triage H-012, implement fallback + late-write H-013 pattern observed then cleaned — test error).
  - Y definitions: Y2 (main, delivered) = `F2-Y2 exact line alpha\nF2-Y2 exact line beta\nF2-Y2 exact line gamma` (67B, no trailing newline — as written by implement and accepted by reviewer; prompt listed lines with trailing newline ambiguity resolved to delivered bytes). Y-old (bug injected, pre-existing) = `F2-Y exact line one/two/three` (overwritten).
- FAILs/rollbacks: none (no code changed; nothing to revert). Test-side gaps only (prompt/worktree setup on jobs 1-4, late-file cleanup on job4 — QA self, fixed in-run; no Engineer action). Source-side Known Issue only (createdFiles folder vs strict file helper — see Carry-over; no hardening in F2 because owning files `implementService`/`minimalChange` are outside F2 guard list `reviewService`/`WorkItemList`; default untouched per plan). One infra `ask_human` recovered on retry — not a FAIL (honest 0-findings + actionable summary, E2E-04 pattern).
- New LOOPS rows + meta-test: +0-note. F2 premieres existing code; no new intervals/retries/dedupe (review retry uses existing `retry-review` route; no new loop). `docs/LOOPS.md` and `tests/no-unbounded-loops.test.ts` untouched by design (§0.1 F2 +0/+0/+0).
- Quota spent: 19 LLM calls total (job1 2 + job2 2 + job3 2 + job4 3 + probe job5 4 + main job6 6; all `sin tarifa`). F2 estimate 6-12 exceeded due to 3 setup jobs + 1 probe (documented learning: parser `en` terminator + `.git` requirement); cumulative (F1 0 + F2 19) still <50, no confirmation needed. No further burn (stop; no additional retries).
- Carry-over (scope-freeze items, never done silently here):
  1. Revise→rebuild loop still unpremiered live (0 natural revises in 6 jobs; main job accept-direct with correct file). Needs genuine defect → revise (rodaje with demanding prompts or injected bug that survives implement first try). Carry to F6 full run / dedicated rodaje (with confirmation if >50). Not a source FAIL (reviewer correctly accepted correct file).
  2. `createdFiles` folder vs file for folder+file prompts (`["lab2-rev"]` while file `lab2-rev/dato.txt` delivered; verify trivial-folder path). Tracking imprecise but delivery correct (no literal, no ghost). Owning fix (record file alongside/instead of folder for folder+file intents) belongs to implement/verify phase (F5 teeth or backlog), NOT F2 (guard files are `reviewService`/`WorkItemList` only). Helper strictness (`assertCreatedFilesExact` file-only) vs folder deliveries also noted for F5 asserts.
  3. Harness `Crear sesión` disabled with no active project in isolated Playwright context (needs `window.termcanvas.projectStore.activeProject` or zustand project; creation via browser fetch used instead — snapshot+disk asserts intact per E1). UI project wiring is out of F2 scope.
  4. Worktree setup learning (for future E2E): implement LLM path requires `.git` or `package.json` in worktree (`implementAgent.ts:91-102`); folder-intent parse needs Spanish `con un archivo` + `en` terminator in first line (`minimalChange.ts:639-663`); empty worktrees fail verification (need passing `test`/`build` or trivial-folder path). Documented here, no code change.
  5. Dirty-tree note (from F0/F1, still true): factory work on disk untracked in git (`HEAD 15fece3e` predates it); F-phase commits remain orchestrator call.
- QA verdict: E2E-02 PASS-direct (job-mto1oxqg-jcdd; delivery correct, accept, budget ok; revise not observed — carried) · E2E-06 PASS (same-session proof, reused) · E2E-07 DIFERIDO-honest (never doubted, no reverify — absence not FAIL) · F2 DONE (premiere process complete, report + asserts + live accept with session proof; revise→rebuild carried to F6 rodaje as Known Absence, not hidden). Routing decision: NoOne (no source bug requiring Engineer; no test bug left unfixed in-run; Known Issue + absence carried, not endlessly debugged per 2-round rule).
