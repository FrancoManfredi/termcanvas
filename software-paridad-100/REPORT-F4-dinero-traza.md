# F4 Report — Real money + clear trace (live assert + trace-half verdict)

- Date (UTC): 2026-09-05 · Engineers: F4-T1/T2 owners (prior) · QA: Edward (software-qa-engineer) · buildId: `15fece3e` · HEAD: `15fece3e`
- Scope: F4-T3 ONLY (live assert, zero code — `REPORT-F4-dinero-traza.md` is the single new file).
- Preconditions:
  - F3 verdict recorded (`REPORT-F3-measure-humano.md`: A DIFERIDO-honest, B DIFERIDO-honest, C DIFERIDO-honest; cumulative 41/50) — PASS.
  - F4-T1/T2 code in place (CostRateSchema + basis/ratesRef, ScoreReason additive, CostBadge sin-tarifa path, ScorersPanel trace strings) — PASS (read, not changed).
  - Daemon user-started, `buildId == HEAD` (`15fece3e`), port 17680, `opencode healthy` — PASS (read-only GETs, 15s, never restarted by QA; daemon itself restarted ~12:58Z by operator — in-memory costTracker recounts from the restart, timelines on disk persist per-lifetime cumulative counts; accounted for honestly below).
  - `factory validate` exit 0 before asserts — PASS (`definition valida: 0 errores, 0 warnings, buildId=15fece3e`).
  - Offline suites green (see DONE) — PASS.
  - User tariffs: NONE supplied → trace-half only per plan §5-assumption-6 (`costRates:{}` stays VALID, means `sin tarifa`; no placeholder committed as data) — PASS (honest DONE scope).
  - Quota: F4 live budget 0-8, cumulative headroom 9 (41/50) — PASS (spent 1, cumulative 42/50).
  - God 3583 (F1 value; F4 allows +0) — PASS.
  - Playwright MCP: browser-singleton lock, 1 probe this phase (9th consecutive session-wide) — BLOQUEADO-infra for the observation layer only (screenshot absent; GETs 15s + disk + code asserts stand per task order; 0-quota retry pending).
- Steps executed (all `-Encoding utf8` reads, HTTP 15s, suites ≤120s, validate ≤30s; NEVER restart/taskkill/`pnpm dev`/`pnpm build`; no code touched):
  1. `GET /factory/health` (15s) → 200, `buildId 15fece3e == HEAD`, `opencode healthy` (port 20605, uptime 257870ms; daemon uptime 581671ms ≈ restart ~12:58Z by operator).
  2. Offline: `cost-rates-live` 11/11, `score-reason-display` 9/9, `measure-scorer` 27/27, `measure-domains` 18/18, `measure-scorers-agents` 16/16, `scorers-ui + measure-benchmark-scorers` 34/34. `npx tsc --noEmit` 0 errors. `node scripts/factory.mjs validate` exit 0. God 3583→3583 (+0).
  3. `GET /factory/scorers` → 3 scorers, allowlist model `opencode-go/muse-spark-1.2-contributor`, `samplingRate: 25`. `GET /factory/scores/summary` → 1 scored / 1 passing / 0 failing per scorer (baseline visible live).
  4. `GET /factory/jobs` → 293 total (Triage 35, Complete 237, Foreman 2, Cancelled 15, Building 2, Intake 2).
  5. Badge-honesty live (E2E-13 core): `GET /factory/jobs/job-mto1nbpw-hjrk/scores` → 3/3 scored (labels en-scope/valido/honesta, all passing, model allowlisted, origin sampled, at 2026-09-05T07:11:42/45/56Z — F2 accept-path evidence, pre-F4 legacy shape with NO `scoreReason` key: honest `unscored-legacy` reads). Disk `scores.json` raw confirms: zero `scoreReason` keys (history untouched).
  6. E2E-08 live re-run (human-triggered scoring, quota 0-8): `POST /factory/jobs/job-mto1nbpw-hjrk/scores/implement-scope-1-3` body `{}` → client-side 15s timeout (judge needs longer; synchronous manual-score POST exceeds the plan-mandated 15s client cap — wart documented, not a source FAIL: panel uses 3s fire-and-forget + re-poll by design). Server-side the judge DID run: new timeline event `2026-09-05T13:10:57.594Z | cost: 1 llamadas · ~717 tokens · sin tarifa (estimated-chars/4)` (1 LLM call burned → cumulative 42/50). After 30s + 45s polls: scores unchanged (old `at` stamps), no new score event, disk mtime still 2026-09-05T07:11:56.68Z. Cause indistinguishable from outside (client-abort cancel vs judge-down silent-drop — judge-down persists nothing BY DESIGN per suite). No retry (1 call each; stop per 2-strike rule). Fresh `scoreReason:"scored"` on disk NOT proven live — claimed as DIFERIDO, not PASS.
  7. E2E-13 live re-run: F4-window jobs all show `llmCalls>0` + honest fields — `job-mtocw5i0-f356` (2 cost events, last `2 llamadas · ~2643 tokens · sin tarifa`), `job-mtocyhlj-jm0y` (3 events, last `3 llamadas · ~3844 tokens · sin tarifa`), `job-mtod2avv-rqzi` (3 events, metas `llmCalls 1/2/3`, `estimatedUSD null`, `basis estimated-chars/4`, `ratesRef null`), `job-mto1nbpw-hjrk` (`llmCalls 1`, `estimatedUSD null`, `basis estimated-chars/4`, `ratesRef null`). Timeline grep for `USD 0.00|~USD` over run-job timelines = 0 hits (NEVER 0.00 as data — holds live). Note: daemon restart ~12:58Z reset in-memory `costSummary` for pre-restart jobs (list shows nulls for old Completes); per-lifetime timeline cost events remain the durable record — E2E-13 asserted on jobs with in-window LLM activity, all `llmCalls>0`, zero counter-FAILs.
  8. Trace-classification live (0 quota): `GET /factory/jobs/job-mtod2avv-rqzi/scores` → `{}` (absent) + offline `shouldSampleJob(job-mtod2avv-rqzi, 25) = false` (OUT) → absent consistent with sampled-out by design (job also pre-Review, so not-applicable would equally apply if attempted — the panel names all three without inventing one, which is the correct honest behavior). `shouldSampleJob(job-mto1nbpw-hjrk, 25) = true` (IN) + 3/3 scored → scored path live. judge-down: no live instance forced (breaking the judge on purpose = theater — refused); covered offline (mock-null → `judge-down`, persists nothing, 9/9 suite).
  9. Playwright: 1 snapshot probe → same singleton lock (`Browser is already in use ... use --isolated`) → badge screenshot BLOQUEADO-obs (9th consecutive session-wide). Fallback: harness `GET /factory-harness.html` serves 200 (shell; badge renders client-side from bundle, not assertable via GET — honest limit); badge text asserted via server-composed timeline cost strings (exact data the badge renders) + `CostBadge.tsx` never-0.00 paths read (null/undefined/non-finite USD → `sin tarifa`; 4-decimal `~USD Y (est.)` only with a real rate; 0 calls → `sin datos todavía`, never `USD 0.00`). 0-quota browser retry pending.
  10. `factory/factory.yaml`: `costRates: {}` + F4 comment block read (no tariffs committed — correct without user numbers); `samplingRate: 25`; `costTracking: true`.
- DONE criteria results (plan §F4):
  - USD badge live with basis: TRACE-HALF DONE — `sin tarifa` + `basis=estimated-chars/4` + `ratesRef=null` served live on every job with `llmCalls>0`; zero `0.00`/`~USD` strings; money-half (real `~USD Y (est.)`) needs user-confirmed tariffs — carry-over, not FAIL (plan §5-assumption-6: trace-half PASS without user tariffs is honest DONE).
  - `ratesRef` on disk: DONE (null honestly, pinned NOT NULL together with USD by `CostSummarySchema` both ways — 11/11 suite).
  - Reason strings live: DONE — `sampled-out (25%)` / `judge-down` / `not-applicable` distinct in `ScorersPanel.tsx` (trace line + absent-hint), `scoreReasonDisplay` + `scoreReasonForOutcome` pure and tested; legacy disk entries keyless (read as `unscored-legacy`).
  - Old scores read as `unscored-legacy`, raw bytes unchanged: DONE live (`scores.json` of `job-mto1nbpw-hjrk` keyless before AND after the re-score attempt; mtime untouched) + offline (9/9 display-only test asserts byte equality after rollout reads; P1.7 recompute intact).
  - Suites: `cost-rates-live` 11/11 + `score-reason-display` 9/9 + sampler vectors (`measure-scorer` 27/27 incl. OUT-56/IN-9 determinism, `measure-domains` 18/18) + scorer suites (`measure-scorers-agents` 16/16, `scorers-ui + measure-benchmark-scorers` 34/34) — DONE. `tsc` 0 — DONE. `validate` exit 0 — DONE. God +0 (3583→3583) — DONE.
  - E2E-08 re-run: PASS on hook/scores-served + baseline (3/3 scores + 1/1/1 summary via GET); DIFERIDO on fresh-`scoreReason`-on-disk (1 call burned, no persist; cause outside-distinguishable; NOT a source FAIL).
  - E2E-13 re-run: PASS (all in-window run jobs `llmCalls>0` with honest badge data; zero `0.00`-as-data).
  - Badge screenshot: BLOQUEADO-obs (fallback asserts stand; 0-quota retry pending).
- Live evidence — money/trace:
  - `job-mto1nbpw-hjrk` (F2 accept-path, dir `C:\tmp\demo-e2e-f2-rev-20260905-040158\.agents\factory\job-mto1nbpw-hjrk`): `costSummary {llmCalls: 1, estimatedInputTokens: 711, estimatedOutputTokens: 6, estimatedUSD: null, basis: estimated-chars/4, ratesRef: null}`; timeline `2026-09-05T13:10:57.594Z | Complete->Complete | cost: 1 llamadas · ~717 tokens · sin tarifa (estimated-chars/4)` (F4 re-score judge call); scores 3/3 legacy (keyless, `at` 07:11:42/45/56Z); disk `scores.json` mtime 2026-09-05T07:11:56.68Z (untouched by the attempt).
  - `job-mtocw5i0-f356`: 2 cost events, last `2 llamadas · ~2643 tokens · sin tarifa (estimated-chars/4)`, 0 USD-badge strings.
  - `job-mtocyhlj-jm0y`: 3 cost events, last `3 llamadas · ~3844 tokens · sin tarifa (estimated-chars/4)`, 0 USD-badge strings.
  - `job-mtod2avv-rqzi`: cost metas `llmCalls 1/2/3`, `estimatedUSD null`, `basis estimated-chars/4`; scores `{}`; sampler OUT(25) → absent consistent with sampled-out.
  - Baseline: `GET /factory/scores/summary` → `{implement-scope-1-3: 1/1/0, review-formato-valido: 1/1/0, verification-honesta: 1/1/0}`.
  - Screenshots/snapshots: none (Playwright BLOQUEADO-obs, lock quoted above).
- FAILs/rollbacks: none (zero code changed; nothing to revert). No source bug proven (re-score no-persist is abort-vs-judge-down indistinguishable from outside; sync-POST-vs-15s-cap is a client-timeout wart, panel pattern already fire-and-forget; NOT routed to Engineer). No test bug (all suites green before and after).
- New LOOPS rows + meta-test: +0-note (assert-only phase; no new intervals/retries/dedupe; poll sleeps are bounded waits, not loops). `docs/LOOPS.md` and `tests/no-unbounded-loops.test.ts` untouched by design (§0.1 F4 +0/+0/+0).
- Quota spent: 1 LLM call (F4 re-score judge for `implement-scope-1-3` on `job-mto1nbpw-hjrk`). F4 budget 0-8 — WITHIN (1). Cumulative F1 0 + F2 19 + F3-A 7 + F3-B 7 + F3-C 8 + F4 1 = 42/50. Headroom 8 for F5/F6.
- Carry-over (scope-freeze items, never done silently here):
  1. Money-half: real `~USD Y (est.)` badge needs user-confirmed tariffs in `costRates` (numbers, never invented). Until then `sin tarifa` honesty IS the DONE state.
  2. Fresh `scoreReason:"scored"` on disk unproven live (offline-proven 9/9 with mock judge). Cheapest live proof: retry manual score when daemon queue drains (pending 39 at assert time — judge may have queued behind load) with a >15s-tolerant observer (panel 3s fire-and-forget + re-poll IS that observer; 0 extra quota if the pending judge completes). Else carry to F6 full run (a natural accept re-scores and persists reason).
  3. Badge screenshot + ScorersPanel reason-strings screenshot: 0-quota Playwright retry when the browser singleton frees (observation-only; asserts already stand on GETs + disk + code).
  4. Sync manual-score POST vs 15s client cap: candidate hardening for a future phase (async accept-then-poll or honest 202 + `Location`), owned outside F4 — NOT fixed here.
  5. Dirty-tree note (still true): factory work on disk untracked in git (`HEAD 15fece3e` predates it); F-phase commits remain orchestrator call. This report file is the single new tracked artifact of F4-T3.
- QA verdict: F4 PASS (trace-half DONE honesto per plan §5-assumption-6 + §F4: rates-schema validated, `costRates:{}` committed with no invented numbers, `sin tarifa` + basis live on every job with `llmCalls>0`, reason strings distinct live-in-code + offline-green, legacy history byte-untouched, E2E-08 scores-served PASS, E2E-13 PASS, suites/tsc/validate/God all green). Routing decision: NoOne (no Engineer action; no QA self-fix pending; money-half + screenshot + fresh-reason-disk are carried Known Items, not hidden).
