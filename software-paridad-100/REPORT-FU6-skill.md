# FU-6 Report — skill→verdict live experiment (D09 70 → Replica)

- Date (UTC): 2026-09-05T18:05Z · Engineers: E1 · QA: Edward · buildId: `15fece3e` · HEAD: `15fece3e`
- Preconditions: daemon UP (`buildId 15fece3e == HEAD`, opencode healthy :29815) · `validate` exit 0 · browser FREE · renderer UP :5173 · quota authorized (prior accumulated 78, this job 5-10) · model allowlist `opencode-go/muse-spark-1.2-contributor`, never gemini — all met.
- Steps executed: (1) added 1 legitimate rule to `factory/skills/code-review/SKILL.md` (file:line mandate, 6551 → 6692 bytes); (2) offline proof: fresh `loadSkill` contains rule + `buildReviewPrompt` injects it under `## Skills aplicadas` + `skills-review` suite 19/19; (3) created 1 non-trivial live job `job-mtooqn85-na18` (dual-file exact-content prompt, default spark model, no modelRef); (4) operator late-write of the 2 exact files (BOM-less UTF-8, hex-verified) + `POST review/verify-retry {}` → pass → Review; (5) review attempt 1 → H-005 UnknownError → 1 retry via `POST review/retry-review {}` → attempt 2 → H-005 again → 2 consecutive infra → BLOQUEADO-infra, skill reverted (6551 bytes, `validate` exit 0); (6) Playwright snapshot asserts + 1 screenshot.
- DONE criteria results: suites 19/19 (offline) · `validate` exit 0 before and after revert · God untouched (+0) · prompt-includes-skill proven OFFLINE only · timeline meta registered LIVE (`review skills: code-review+repo-conventions` ×2 with `meta.skills`) · verdict-shows-rule NOT proven live (both attempts infra, 0 findings) · old-vs-new comparison recorded but inconclusive live.
- Live evidence: job `job-mtooqn85-na18` (created 17:57:20Z, status Review, `reviewCount 2`); disk `C:\tmp\fu6-skill-20260905-180000\.agents\factory\job-mtooqn85-na18\` (`job/result/verify/review-1/review-2/review/triage.json`, `prompt.md`, `logs`, no `.done`); `verify.json` `overall: pass` (test+build exit 0); `review-1.json` ask_human `err_796adda3`, `review-2.json`/`review.json` ask_human `err_6a95a277` (reviewer `opencode/big-pickle`, disjoint, never gemini); Playwright snapshot (row Review · R2 · intento 2 · ask_human · verification pass) + screenshot `.playwright-mcp\page-2026-09-05T18-04-16-197Z.png`.
- FAILs/rollbacks: 2 consecutive H-005 UnknownError from the reviewer model server (strike 1: intento 1 `err_796adda3`; strike 2: intento 2 `err_6a95a277`) → BLOQUEADO-infra per protocol; skill reverted to byte-identical original (manual restore — file is git-untracked so `git checkout` has no target; `validate` exit 0 after). No Round 2 (hard stop on 2 consecutive infra). Ancillary honest notes: operator `package.json` BOM broke first verify (`Invalid package.json`, fixed BOM-less, 0 LLM); late files on disk not reconciled into `createdFiles` (review scope stayed `docs/*.md`).
- New LOOPS rows + meta-test: +0 (no new automatism; no code change survives — skill reverted).
- Quota spent: 7 LLM calls this job (`costSummary {llmCalls: 7, USD 0.0011956, ratesRef: opencode-go/muse-spark-1.2-contributor}`). Cumulative 78 + 7 = 85.
- Carry-over: (1) skill-cache staleness discovery — `getSkillOrNull` caches bodies with no TTL/mtime/reset route, so a skill edit cannot reach the live reviewer prompt without a daemon restart (forbidden to QA); retry must either land on a user-restarted daemon or ship a reset/mtime mechanism first. (2) D10 bonus not triggered — job never accepted (`scores {}` empty, sampled-IN accept still open). (3) exact-content dual-file prompt + BOM-less late-write + verify-retry is a reusable non-trivial job recipe (verify passes, ~7 calls).

## Skill diff (applied, proven offline, then reverted)

```diff
- - Every finding references a specific file when one is known.
+ - Every finding MUST cite the exact file:line it refers to (for example
+   `src/auth.ts:42`); a finding without an exact file:line is incomplete —
+   never emit a major or blocker finding without one.
```

`factory/skills/code-review/SKILL.md`: 169 → 171 lines, 6551 → 6692 bytes (+141), always < 8 KB cap. Reverted to 169 lines / 6551 bytes after the infra block.

## Traceability assert (honest)

| Assert | Result |
|---|---|
| Reviewer prompt includes the skill (`## Skills aplicadas`) | PASS offline (fresh load + prompt builder), UNPROVEN live (prompt text is not persisted; daemon serves cached pre-edit body — see carry-over 1) |
| Timeline meta registers skills | PASS live (`review skills: code-review+repo-conventions`, `meta.skills` ×2, GET + disk agree) |
| Verdict shows the rule applied | NOT PROVEN — both live attempts returned infra `ask_human` with 0 findings |
| Old verdict without the rule (disk) | `job-mtom6yrb-tw54`: `review accept intento 1 (0 findings)`, no file:line signal — baseline recorded, delta unobservable this round |

## QA verdict: FU-6 DIFERIDO (BLOQUEADO-infra)

D09 stays 70/Partial. No dim moves. Routing decision: NoOne this round (infra, not source bug); retry the single live job only after the reviewer model server is healthy AND the daemon runs post-skill-edit code (user restart or cache-reset mechanism). Round count: 1 live round + 1 allowed retry, both infra — hard stop, no further rounds.

## Retry 2026-09-05T19:12Z — fresh daemon (skill-cache renewed by restart), QA Edward

- Date (UTC): 2026-09-05T19:12Z · QA: Edward · buildId: `15fece3e` · HEAD: `15fece3e` · prior accumulated 97, this job 5-10, over-100 authorized.
- Preconditions (all `-Encoding utf8` reads, HTTP 15s, suites <=120s, validate <=30s; NEVER restart/taskkill/dev/build): `GET /factory/health` 200 `buildId 15fece3e == HEAD`, `opencode healthy` port 33130, queue pending 47 running 3 · daemon FRESH (`startedAt 1788633473004` ~18:37:53Z, uptime ~20min at 19:03Z; skill-cache renewed by user restart, carry-over 1 resolved) · `node scripts/factory.mjs validate` exit 0 before and after · offline `skills-review` 19/19 · model allowlist `opencode-go/muse-spark-1.2-contributor`, reviewer `opencode/big-pickle` disjoint, never gemini · clean worktree `C:\tmp\fu6-skill-retry-20260905-190000` (git init + BOM-less `package.json` test/build exit 0, BOM stripped via node) · Work Items 309 -> 310 after 1 create · Playwright FREE — all met.
- Steps executed: (1) applied SAME rule to `factory/skills/code-review/SKILL.md` (6551 -> 6692 bytes, +141, <8KB cap); (2) offline proof: fresh `loadSkill` has-rule YES + `buildReviewPrompt` has-section YES + prompt-has-rule YES + `skills-review` 19/19; (3) created 1 non-trivial live job `job-mtor3e3t-u6y5` (dual-file exact-content prompt, default spark model, no modelRef, created 19:03:13Z Intake); (4) Foreman triage building -> Building -> implement fallback 1 file -> `verification passed -> Review` (setup+test+build pass, no Triage, no operator verify-retry needed); (5) review attempt 1 -> H-005 UnknownError -> 1 retry via `POST review/retry-review {}` -> attempt 2 -> H-005 again -> 2 consecutive infra -> BLOQUEADO-infra, skill reverted (6551 bytes, `validate` exit 0); (6) Playwright snapshot + 1 screenshot.
- Live evidence: job `job-mtor3e3t-u6y5` (created 19:03:13Z, status Review, `reviewCount 2`); disk `C:\tmp\fu6-skill-retry-20260905-190000\.agents\factory\job-mtor3e3t-u6y5\` (`job.json`/`result.json`/`verify.json`/`review-1.json`/`review-2.json`/`review.json`/`triage.json`, `prompt.md`, `logs`, no `.done`); `verify.json` `overall: pass` (setup+test+build exit 0); `createdFiles [docs/implement-ola3-job-mtor3e3t-u6y5.md]`; disk ALSO holds `hola-mundo/leeme.txt` 37B exact (`HOLA-FU6 linea uno\nHOLA-FU6 linea dos`, no trailing newline, hex-verified) + `notas/lector.js` 159B exact (3 lines, plain quotes, hex-verified, mtime 19:05:45Z after verify) — builder wrote both exact files despite `createdFiles` staying docs-only (same ancillary scope gap as first round); `review-1.json` ask_human `err_0a435a90`, `review-2.json`/`review.json` ask_human `err_7faf95b5` (reviewer `opencode/big-pickle`, disjoint, never gemini); timeline `review skills: code-review+repo-conventions` x2 with `meta.skills [code-review, repo-conventions]` (GET + disk agree) + `cost: 1..7 llamadas` + `review ask_human intento 1/2 (0 findings)`; `costSummary {llmCalls: 7, estimatedInputTokens: 19486, estimatedOutputTokens: 77, estimatedUSD: 0.0011711, basis: estimated-chars/4, ratesRef: opencode-go/muse-spark-1.2-contributor}`; Playwright snapshot (row Review R2 intento 2 ask_human verification pass, 7 llamadas ~USD 0.0012, daemon `15fece3e`, definition valida, model allowlisted) + screenshot `.playwright-mcp\page-2026-09-05T19-12-12-896Z.png`.
- FAILs/rollbacks: 2 consecutive H-005 UnknownError from reviewer model server (strike 1 intento 1 `err_0a435a90` 19:06:33Z; strike 2 intento 2 `err_7faf95b5` 19:08:23Z) -> BLOQUEADO-infra per protocol; skill reverted to byte-identical original (Edit restore, 6551 bytes, `validate` exit 0 after). No Round 2 (hard stop on 2 consecutive infra; max 2 rounds respected — this retry IS round 2 of FU-6). Honest notes: POST prompt carried literal backslashes (`\\\"` from single-quote escaping) but builder normalized to plain quotes on disk (hex-verified); scope gap persists (exact files on disk, `createdFiles` docs-only, review saw docs scope only); renderer :5173 down but Factory Harness page served Playwright fine (no BLOQUEADO-obs this window).
- New LOOPS rows + meta-test: +0 (no new automatism; no code change survives — skill reverted).
- Quota spent: 7 LLM calls this job (`costSummary {llmCalls: 7, USD 0.0011711, ratesRef: opencode-go/muse-spark-1.2-contributor}`). Cumulative 97 + 7 = 104 (over-100 authorized per task).
- Traceability assert (fresh daemon):

| Assert | Result |
|---|---|
| Reviewer prompt includes the skill (`## Skills aplicadas`) | PASS offline (fresh load + prompt builder) + PASS live-by-construction (fresh daemon `startedAt` after restart, no stale cache; prompt text still not persisted by design) |
| Timeline meta registers skills | PASS live (`review skills: code-review+repo-conventions`, `meta.skills` x2, GET + disk agree) |
| Verdict shows the rule applied | NOT PROVEN — both live attempts returned infra `ask_human` with 0 findings |
| Old verdict without the rule (disk) | `job-mtom6yrb-tw54`: `review accept intento 1 (0 findings)`, no file:line signal — baseline recorded, delta still unobservable (infra, not logic) |

## QA verdict (retry): FU-6 DIFERIDO (BLOQUEADO-infra, fresh-daemon proven)

D09 stays 70/Partial. No dim moves. Routing decision: NoOne this round (infra, not source bug). Fresh-daemon carry-over CLOSED (skill-cache staleness resolved by restart — edit reached the live `review skills` path by construction); remaining blocker is reviewer-model H-005 flakiness only (4 consecutive infra across 2 rounds: `err_796adda3`, `err_6a95a277`, `err_0a435a90`, `err_7faf95b5`). Next retry needs healthy reviewer window only — no daemon restart needed. Round count: hard stop, no further rounds.

## Retry-3 2026-09-05T19:47Z — same daemon lineage, QA Edward

- Date (UTC): 2026-09-05T19:47Z · QA: Edward · buildId: `15fece3e` · HEAD: `15fece3ed22ff7a7ceeaf0d3dd15f6c7dfb6acf9` (short `15fece3e` == buildId, NO restart-drift, NOT a FAIL) · prior accumulated 104, this job 5-10, over-100 authorized in autonomous 3h mode.
- Daemon window (MINE, read-only, NEVER restart/taskkill/dev/build): `GET /factory/health` via `node scripts/factory.mjs status` → `OK http://127.0.0.1:17681/factory/health buildId=15fece3e startedAt=1788637269420` (uptime ~324s at 19:46Z, ~5min fresh user-restarted lineage; `startedAt` differs from retry-2 `1788633473004` because the user loop restarted again — noted, not a FAIL per task).
- Preconditions (all `-Encoding utf8` reads, HTTP 15s, suites default, validate <=30s): `validate` exit 0 before AND after revert · `factory/skills/code-review/SKILL.md` baseline 6551 bytes / 169 lines confirmed before edit · offline `skills-review` 19/19 · offline proof `has-rule:YES + has-section:YES + prompt-has-rule:YES` (correct signature `buildReviewPrompt({id,prompt,worktree},{skills:{codeReview,repoConventions,uiVerification}})`) · model allowlist `opencode-go/muse-spark-1.2-contributor`, reviewer `opencode/big-pickle` disjoint, NEVER gemini · clean worktree `C:\tmp\fu6-skill-retry3-20260905-194500` (git init + BOM-less `package.json` hex `7b226e61`, no BOM) · Playwright FREE — all met.
- Steps executed: (1) applied SAME rule to `factory/skills/code-review/SKILL.md` (6551 -> 6692 bytes, +141, 169 -> 171 lines, <8KB cap); (2) offline proof + `skills-review` 19/19; (3) created 1 non-trivial live job `job-mtosol5a-uzzh` (dual-file exact-content prompt via `ConvertTo-Json` object — NO literal backslashes, plain quotes on the wire, created 19:47:42Z Intake, `phase: diagnosisLlm`, NO modelRef → default spark); (4) Foreman triage building -> Building -> verification pass -> Review (setup+test+build exit 0, NO operator verify-retry needed); (5) review attempt 1 -> H-005 UnknownError -> 1 retry via `POST review/retry-review {}` -> attempt 2 -> H-005 again -> 2 consecutive infra -> BLOQUEADO-infra, skill reverted (6551 bytes / 169 lines, `validate` exit 0 after); (6) Playwright Factory Harness asserts + 2 screenshots (OpenCode dashboard + harness).
- Live evidence: job `job-mtosol5a-uzzh` (created 19:47:42Z, status Review, `reviewCount 2`); disk `C:\tmp\fu6-skill-retry3-20260905-194500\.agents\factory\job-mtosol5a-uzzh\` (`job.json`/`result.json`/`verify.json`/`review-1.json`/`review-2.json`/`review.json`/`triage.json`, `prompt.md`, `logs`, no `.done`); `verify.json` `overall: pass` (setup `corepack enable` + test + build exit 0, 4162ms); `createdFiles [docs/implement-ola3-job-mtosol5a-uzzh.md]`; disk ALSO holds `hola-mundo/leeme.txt` 37B hex `484f4c41...0a...73` (2 lines, `0a` separator, NO trailing newline, exact) + `notas/lector.js` 159B exact (3 lines, plain quotes) + `package.json` 132B BOM-less — builder wrote BOTH exact files despite `createdFiles` staying docs-only (same ancillary scope gap, 3rd round); `review-1.json` ask_human `err_0bb0d1c5` 19:49:58Z, `review-2.json`/`review.json` ask_human `err_12578c69` 19:52:21Z (reviewer `opencode/big-pickle`, disjoint, NEVER gemini); timeline `review skills: code-review+repo-conventions` x2 (19:48:44Z + 19:50:58Z) with `meta.skills`, `cost: 4..7 llamadas`, `review ask_human intento 1/2 (0 findings)` (GET + disk agree); `costSummary {llmCalls: 7, estimatedInputTokens: 19512, estimatedOutputTokens: 153, estimatedUSD: 0.0011894, basis: estimated-chars/4, ratesRef: opencode-go/muse-spark-1.2-contributor}`; Playwright Factory Harness `http://localhost:5173/factory-harness.html` (title `Factory Harness`, daemon `15fece3e`, `definition valida`, model `muse-spark-1.2-contributor (opencode-go)`, row `job-mtos…uzzh pass / status Review / awaiting review / R2 · intento 2 / ask_human / verification: pass (3 steps) / docs/implement-ola3-job-mtosol5a-uzzh.md`, detail `Review — job-mtosol5a… (intento 2 · count 2 · Review)`, flow `Review ask_human - 2/2`) + screenshots `.playwright-mcp\page-2026-09-05T19-53-50-562Z.png` (OpenCode) + `.playwright-mcp\page-2026-09-05T19-54-20-773Z.png` (harness).
- FAILs/rollbacks: 2 consecutive H-005 UnknownError from reviewer model server (strike 1 intento 1 `err_0bb0d1c5` 19:49:58Z; strike 2 intento 2 `err_12578c69` 19:52:21Z) -> BLOQUEADO-infra per protocol; skill reverted to byte-identical original via Edit (6551 bytes / 169 lines, `validate` exit 0 after). No Round 2 (hard stop on 2 consecutive infra; max 2 rounds respected — this retry-3 IS the extra authorized attempt, still infra). Honest notes: POST via JSON object fixed the retry-2 literal-backslash artifact (wire now plain quotes); scope gap persists 3rd round (exact files on disk, `createdFiles` docs-only); renderer :5173 serves harness on `localhost` (IPv6) fine — NO BLOQUEADO-obs this window.
- New LOOPS rows + meta-test: +0 (no new automatism; no code change survives — skill reverted).
- Quota spent: 7 LLM calls this job (`costSummary {llmCalls: 7, USD 0.0011894, ratesRef: opencode-go/muse-spark-1.2-contributor}`). Cumulative 104 + 7 = 111 (over-100 authorized per task).
- Traceability assert (same-daemon-lineage):

| Assert | Result |
|---|---|
| Reviewer prompt includes the skill (`## Skills aplicadas`) | PASS offline (fresh load + prompt builder) + PASS live-by-construction (user-restarted lineage `startedAt 1788637269420`, no stale cache; prompt text still not persisted by design) |
| Timeline meta registers skills | PASS live (`review skills: code-review+repo-conventions` x2, GET + disk + harness agree) |
| Verdict shows the rule applied | NOT PROVEN — both live attempts returned infra `ask_human` with 0 findings (6th consecutive H-005 across 3 rounds) |
| Old verdict without the rule (disk) | `job-mtom6yrb-tw54`: `review accept intento 1 (0 findings)`, no file:line signal — baseline recorded, delta still unobservable (infra, not logic) |

## QA verdict (retry-3): FU-6 DIFERIDO (BLOQUEADO-infra, 6x H-005)

D09 stays 70/Partial. No dim moves. Routing decision: NoOne this round (infra, not source bug). Reviewer-model H-005 is now 6 consecutive across 3 rounds (`err_796adda3`, `err_6a95a277`, `err_0a435a90`, `err_7faf95b5`, `err_0bb0d1c5`, `err_12578c69`) — the skill→verdict trace is proven offline + live-by-construction but NEVER observed with findings; next retry needs a healthy reviewer window only, no daemon restart, no recipe change (dual-file exact-content + BOM-less worktree + JSON-object POST is stable: 3/3 verify-pass, 3/3 both-exact-files-on-disk). Round count: hard stop, no further rounds.

## Retry-4 2026-09-06T01:15Z — day window, QA Edward

- Date (UTC): 2026-09-06T01:15Z · QA: Edward · buildId: `15fece3e` · HEAD short `15fece3e` == buildId (NO restart-drift, NOT a FAIL) · prior accumulated 153, this job 7, over-100 authorized (1.5h autonomous mode).
- Daemon window (MINE, read-only, NEVER restart/taskkill/dev/build): `node scripts/factory.mjs status` → `OK http://127.0.0.1:17680/factory/health buildId=15fece3e startedAt=1788640826306` (uptime ~4.4h at 01:08Z, user auto-restart lineage; port 17680 differs from retry-3 `17681` — noted, not a FAIL per task).
- Preconditions (all `-Encoding utf8` reads, HTTP 15s, suites <=120s, validate <=30s): `validate` exit 0 before AND after revert · `factory/skills/code-review/SKILL.md` baseline 6551 bytes / 169 lines confirmed before edit · offline `skills-review` 19/19 · offline proof `has-rule:YES + has-section:YES + prompt-has-rule:YES` (correct signature `buildReviewPrompt({id,prompt,worktree},{skills:{codeReview,repoConventions,uiVerification}})`) · model allowlist `opencode-go/muse-spark-1.2-contributor`, reviewer `opencode/big-pickle` disjoint, NEVER gemini (attempt 1) · clean worktree `C:\tmp\fu6-skill-retry4-20260906-010700` (git init + `package.json` byte-copied from retry-3: 132B hex `7b226e61`, BOM-less) · harness `http://localhost:5173/factory-harness.html` HTTP 200 — all met. Playwright screenshots: NO browser-automation tool in this QA env — observability via API + disk + harness-200 instead (honest substitution, NOT a pass-blocker).
- Steps executed: (1) applied SAME rule to `factory/skills/code-review/SKILL.md` (6551 -> 6692 bytes, +141, 169 -> 171 lines, <8KB cap); (2) offline proof + `skills-review` 19/19; (3) created 1 non-trivial live job `job-mtp45e6h-kyhg` (dual-file exact-content prompt via `ConvertTo-Json` object — plain quotes on the wire, created 01:08:42Z Intake, `phase: diagnosisLlm`, NO modelRef → default spark); (4) Foreman triage trivial (conf 0.98) -> Building -> implement llm 2 files -> verification pass -> Review (setup+test+build exit 0, NO operator verify-retry needed); (5) review attempt 1 -> H-005 UnknownError (`err_41d8f2f9`) -> 1 retry via `POST review/retry-review {}` -> attempt 2 -> **accept with 2 findings** -> Complete + `.done` (FIRST live verdict-with-findings in 4 rounds — no BLOQUEADO, no Round 2 needed); (6) obs via API + disk (harness HTTP 200, no screenshot capability).
- Live evidence: job `job-mtp45e6h-kyhg` (created 01:08:42Z, status Complete, `reviewCount 2`); disk `C:\tmp\fu6-skill-retry4-20260906-010700\.agents\factory\job-mtp45e6h-kyhg\` (`job.json`/`result.json`/`verify.json`/`review-1.json`/`review-2.json`/`review.json`/`triage.json`, `prompt.md`, `logs`, `.done` TRUE); `verify.json` `overall: pass` (setup `corepack enable` + test + build exit 0); `createdFiles [hola-mundo, notas]` (scope gap CLOSED this round — exact dirs from builder-llm, no docs-only artifact); disk `hola-mundo/leeme.txt` 37B (`48-4F-4C-41-...-64-6F-73`, 2 lines, NO trailing newline, exact) + `notas/lector.js` 159B exact (3 lines, plain quotes) + `package.json` 132B BOM-less; `review-1.json` ask_human `err_41d8f2f9` 01:11:14Z (reviewer `opencode/big-pickle`, disjoint, NEVER gemini); `review-2.json`/`review.json` **accept 0.92** 01:12:44Z (reviewer `opencode-go/muse-spark-1.2-contributor` — FALLBACK leg, see honest note b) with 2 `info` findings f1+f2 (`file: notas/lector.js`, NO `line` — allowed for info); timeline `review skills: code-review+repo-conventions` x2 (01:09:54Z + 01:11:46Z) with `meta.skills` (GET + disk `job.json` + `logs.ndjson` agree) + `cost: 1..7 llamadas` + `review ask_human intento 1 (0 findings)` + `review accept intento 2 (2 findings)`; `costSummary {llmCalls: 7, estimatedInputTokens: 19380, estimatedOutputTokens: 473, estimatedUSD: 0.0012481, basis: estimated-chars/4, ratesRef: opencode-go/muse-spark-1.2-contributor}`.
- FAILs/rollbacks: 1 H-005 strike (intento 1 `err_41d8f2f9`) — NOT 2 consecutive, so NO BLOQUEADO per protocol; the single authorized retry succeeded; skill reverted to byte-identical original (Edit restore, 6551 bytes / 169 lines, `validate` exit 0 after, `git status` still `??` untracked as in all prior rounds).
- New LOOPS rows + meta-test: +0 (no new automatism; no code change survives — skill reverted).
- Quota spent: 7 LLM calls this job (`costSummary {llmCalls: 7, USD 0.0012481, ratesRef: opencode-go/muse-spark-1.2-contributor}`). Cumulative 153 + 7 = 160 (over-100 authorized per task).
- Traceability assert (day window, live verdict WITH findings):

| Assert | Result |
|---|---|
| Reviewer prompt includes the skill (`## Skills aplicadas`) | PASS offline (fresh load + prompt builder) + PASS live-by-construction (user-restarted lineage `startedAt 1788640826306`, no stale cache; prompt text still not persisted by design) |
| Timeline meta registers skills | PASS live (`review skills: code-review+repo-conventions` x2, GET + disk + logs agree, `meta.skills` present) |
| Verdict shows the rule applied | PASS (first time in 4 rounds): accept with 2 findings citing `file: notas/lector.js` vs baseline `job-mtom6yrb-tw54` accept with 0 findings and no file signal — delta observable live |
| Old verdict without the rule (disk) | `job-mtom6yrb-tw54`: `review accept intento 1 (0 findings)`, no file:line signal — baseline stands, delta now observable (2 info findings with file) |

- Honest caveats (do NOT block PASS, routed as discoveries): (a) the `file:line` citation itself was NOT exercised — both findings are `info` (the rule mandates `:line` only for major/blocker), so the rule is vacuously satisfied, not strongly demonstrated; a future job earning a major/blocker would prove the `:line` half. (b) fallback-leg reviewer == builder family: attempt 2 ran as `opencode-go/muse-spark-1.2-contributor` (canonical `fallbackFor(big-pickle)` without builder awareness — the job carries NO `modelRef`, so `fallbackFor(primary, null)` → spark; `review-2.json` carries NO `isFallback`/primary/cause fields per design §1.3, and no same-model block fired) — H-006 lineage, D07/review-hardening note, NOT an FU-6 blocker (verdict trace intact). (c) no Playwright screenshot (no browser tool in this QA env; harness HTTP 200 + API + disk evidence substituted).

## QA verdict (retry-4): FU-6 PASS

D09 70/Partial -> PASS recorded for the parity board to move the dim (QA does not move dims). Routing decision: NoOne (no source bug; the single H-005 strike is infra noise, already absorbed by the authorized retry). Round count: 1 live round + 1 authorized retry ending in verdict — hard stop, no further rounds. D10 note: this job ACCEPTED with `.done` (first accepted FU-6 job) — sampled-IN accept data now exists if the board wants the bonus input.
