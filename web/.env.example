# GitHub PAT para Fase 1 (build-time). Crear en https://github.com/settings/tokens (classic) con scope `repo`.
# Vite requiere prefijo VITE_ y restart del dev server tras editar este archivo.
VITE_GITHUB_TOKEN=
