import { describe, it, expect } from "vitest";

describe("HelpLink — deleted in replica (placeholder)", () => {
  it("placeholder passes — help removed per PRD kill list", () => {
    expect(true).toBe(true);
  });
});
