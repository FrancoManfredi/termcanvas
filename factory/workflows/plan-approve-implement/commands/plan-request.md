# Plan corto para el pedido del usuario.
# El nodo `plan` declara output_format, por lo que la salida es un objeto JSON.
Armá un plan de implementación breve (3-6 pasos) para el siguiente pedido.

Pedido: $INPUTS.request

Respondé solo el plan, sin preámbulos.
