---
description: Review final — verde solo con criterios cumplidos y verificación pasada
---
Revisá la implementación contra la spec y el triage.

Spec: $spec.output
Verificación: $verify.output

Respondé JSON con `green` (booleano) y `findings` (string). `green` es true
solo si todos los criterios de aceptación están cumplidos y la verificación
pasó sin errores.
