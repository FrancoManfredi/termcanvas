node_modules/
dist/
dist-electron/
dist-cli/
dist-computer-use/
dist-headless/
out/
.claw/
.vite/
native/computer-use-helper/.build/

*.log
*.tsbuildinfo

.DS_Store
Thumbs.db

.env
.env.local

.claude/
.worktrees/
thirdparty/

# Hydra temp files
.hydra-result-*.md
.hydra-task-*.md
.hydra/

# Agent config for other runtimes
AGENTS.md
CLAUDE.md

# Entrevista de requerimientos: ledgers por proyecto (no se commitean)
/.agents/interview/

# Factory runtime state: jobs por worktree, no se commitea
/.agents/factory/

# Copia anidada con estado de runtime que dejaron corridas del factory
/termcanvas/

# Internal implementation plans
docs/plans/

# Playwright MCP artifacts
.playwright-mcp/

# Debug snapshots
git-harness-snapshot.md

# Local workspace state
.termcanvas/
*.termcanvas

# Local-only archives
.local-archive/
docs/research/harness-articles-2026/

# Locally-installed third-party skills (not project-managed). The
# project's own skill plugin lives at /skills/skills and is exposed
# under .agents/skills/termcanvas via a tracked symlink, so we ignore
# every other entry under .agents/skills/ instead of the whole tree.
/.agents/skills/*/
!/.agents/skills/termcanvas
/skills-lock.json
scripts/.tools/
# Factory runtime state (no se commitea)
factory/.job-index.json
factory/.job-index.json.tmp-*
factory/.notifications.json
factory/.automations.json
factory/.settings.json
factory/.settings.json.tmp-*
factory/.integrations-mock.json
factory/.proposals/
factory/.benchmark-results/

# Scratch repo and captures left by local factory test runs
/test-orquestador-work/
/health.json
/snapshot.yaml
/wf-clean.png
/verify_simple.mjs
/verify_simple2.mjs
/playwright-verify.mjs
/playwright-verify2.mjs

# Scratch modules leaked into the checkout by a factory test job (issues #97/#125)
/a11y.ts
/todo.ts
/todo.test.ts
/src/a11y.ts
/src/todo.ts
/tests/a11y-cancel.test.ts